<div class="container mx-auto mt-4">
  <div class="grid grid-cols-1 grid-rows-2">
    <h1 class="text-center text-6xl font-bold">Bienvenid@ {{user.name}}!</h1>
    <div class="grid grid-flow-row">
      {{#if isAdmin}}
        <p class="w-full max-w-5xl justify-self-center text-lg">Es bueno ser
          admin.</p>
      {{else}}
        <p class="w-full max-w-5xl justify-self-center text-lg">No tienes
          permiso de admin.</p>
      {{/if}}
    </div>

  </div>
</div>